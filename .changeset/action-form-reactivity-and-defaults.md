---
"@dyrected/admin": minor
"@dyrected/core": minor
---

Support dynamic and JEXL `defaultValue` evaluation, `admin.hooks.onChange` live reactivity, `admin.hooks.options` cascading dropdowns, and `admin.condition` visibility inside `ActionFormDialog` and `FormEngine`.

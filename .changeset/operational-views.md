---
"@dyrected/core": minor
"@dyrected/sdk": minor
"@dyrected/admin": minor
---

Operational Views: `defineView` / `defineAction` configuration helpers, view-aware admin sidebar and routing, table (tablecn), kanban (@dnd-kit), calendar, and cards layouts, summary metric stat cards backed by the aggregation engine, and an actions runner that resolves declarative mutations (`now()`, `input.*`, `doc.*`) or self-hosted handlers through the standard lifecycle-hook pipeline.

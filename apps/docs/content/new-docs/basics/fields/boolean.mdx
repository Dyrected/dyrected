---
title: Boolean
description: A true/false toggle, shown to editors as a checkbox or switch.
---

The boolean field stores `true` or `false`, shown to editors as a checkbox. Use it for on/off flags such as `featured`, `published`, or `acceptsTerms`. The helper is `defineBooleanField`.

```ts
defineBooleanField({ name: 'featured', label: 'Featured', defaultValue: false })
```

Set a `defaultValue` so new documents start in a known state instead of an empty one.

By default the admin panel renders a checkbox. Switch it to a toggle when that reads better for the flag — both store the same boolean:

```ts
defineBooleanField({
  name: 'published',
  label: 'Published',
  defaultValue: false,
  admin: { layout: 'switch' },
})
```

## Generated reference

The contract below is generated from the public `@dyrected/core` exports by `@dyrected/knowledge`, so it stays in sync with the package. See [Fields](/new-docs/basics/fields/overview) for the shared field options every type accepts.

{/* GENERATED:REFERENCE-FIELD-BOOLEAN:START */}
## BooleanField

A `true`/`false` value, shown to editors as a checkbox or switch.

```ts
export type BooleanField = TypedField<"boolean", boolean, BooleanFieldAdmin>;
```


## BooleanFieldAdmin

Exported type from @dyrected/core.

```ts
export type BooleanFieldAdmin = {
  /** Boolean presentation style. */
  layout?: "checkbox" | "switch";
};
```
{/* GENERATED:REFERENCE-FIELD-BOOLEAN:END */}

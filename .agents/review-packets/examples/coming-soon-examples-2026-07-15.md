# Review Packet: Ecosystem Examples Coming-Soon Pages

## Document purpose

Add lightweight public pages under `/docs/ecosystem/examples/` so the docs can name the planned example applications now, without pretending the example repositories or full walkthroughs already exist.

## Source inventory

- `apps/docs/content/docs/ecosystem/meta.json`
  Why it matters: confirms `examples` is part of the public docs IA.
  Trust: high.
- `apps/docs/content/docs/ecosystem/examples/meta.json`
  Why it matters: current nav surface for the examples section.
  Trust: high.
- `apps/docs/DOCS_PHILOSOPHY.md`
  Why it matters: defines tone and page structure expectations.
  Trust: high.
- User direction in this thread
  Why it matters: establishes the intended examples shape: ecommerce app, booking app, knowledge-base app; use `Patterns used`; example links are not ready yet; `coming soon` is acceptable.
  Trust: high for product direction, low for implementation claims.

## Uncertainty register

- The eventual example repositories, file locations, and public links are not ready yet.
- The final subpage structure for each example is intentionally undecided.
- The exact schema and feature surface for each example app may evolve before implementation starts.

## High-risk claims to verify

- Whether the three initial examples should remain exactly `ecommerce`, `booking`, and `knowledge-base`.
- Whether `Patterns used` should later be generated from `@dyrected/knowledge` metadata or stay authored in MDX.
- Whether each example should stay as a single page until the repositories are published.

## Specific review questions

- Does the examples section feel appropriately lightweight for a `coming soon` state?
- Are the planned pattern lists useful without overcommitting to implementation details?
- Should any of the related-doc links point somewhere more specific before the repositories exist?

## Status

Draft ready for review. No human verification has happened yet.
